Question - 1
-------------

$touch fileCheck.sh
$vi fileCheck.sh
insert the below code into the file :

#!/bin/bash
user=`whoami`
path="/home/$user"
[ -e $path/*.txt ] || echo "No file exist with .txt extension";
[-e $path/*.txt] && rm *.txt;
tar -cvf otherData.tar $path/*





